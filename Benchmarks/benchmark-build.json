{
  "help": {
    "iterations"              : "Number of measurement loops per size/setup. Higher = more stable median.",
    "warmup_iterations"       : "Loops run before measurement to warm up instruction/data caches.",
    "slave_thread_id"         : "CPU core index to pin the benchmark thread to. Isolates noise.",
    "data_types"              : "C++ types to benchmark. Supported: 'float', 'double'.",
    "sizes_vector"            : "Explicit list of vector sizes. If set, overrides size_region_vector.",
    "sizes_matrix"            : "Explicit list of matrix dimensions. If set, overrides size_region_matrix.",
    "size_region_vector"      : "[min, max] range for automatic logarithmic size sampling (Vector).",
    "size_region_matrix"      : "[min, max] range for matrix dimension sampling (Matrix).",
    "unroll_factors"          : "Template unroll factors to compile. e.g. [1, 2, 4].",
    "operations_vector"       : "Vector ops to bench. e.g. 'dot', 'axpy', 'scale', 'sum'.",
    "operations_matrix"       : "Matrix ops to bench. e.g. 'gemv'.",
    "operations_memory"       : "Memory ops to bench. e.g. 'copy', 'setValue'.",
    "available_ram"           : "Available RAM to limit matrix sizes (e.g. '16GB', '8000MB').",
    "data_range_min"          : "Minimum value for random data generation.",
    "data_range_max"          : "Maximum value for random data generation.",
    "seed"                    : "Random seed for reproducibility.",
    "enable_eigen"            : "Compare against Eigen library (if found).",
    "enable_openblas"         : "Compare against OpenBLAS (if found).",
    "enable_papi"             : "Collect hardware counters (FLOPS, L2 misses, etc) via PAPI."
  },
  
  "iterations"              : 20,
  "warmup_iterations"       : 2,
  "slave_thread_id"         : 0,
  
  "data_types"              : ["double"],
  "size_region_vector"      : [256, 20000000000],
  "size_region_matrix"      : [16, 100000],
  "available_ram"           : "64GB",

  "unroll_factors"          : [1, 4],
  "store_policies"          : ["stream", "cache"],

  "operations_vector"       : ["axpy", "dot", "scale", "sum"],
  "operations_matrix"       : ["gemv"],
  "operations_memory"       : ["copy"],

  "data_range_min"          : 0,
  "data_range_max"          : 10,
  "seed"                    : 0,

  "enable_eigen"            : false,
  "enable_openblas"         : false,
  "enable_papi"             : true
}

